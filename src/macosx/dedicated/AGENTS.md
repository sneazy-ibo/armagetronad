# src/macosx/dedicated/ Directory

## Summary
macOS dedicated server-specific build configuration headers.

## Details
Contains `config.h` with preprocessor definitions for the macOS dedicated server build:
- macOS Application Support data directory path for dedicated server
- DEDICATED, KRAWALL_SERVER, and zthread availability flags

## Directory Structure

```
.
└── config.h    # macOS dedicated server build configuration
```
