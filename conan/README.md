# Conan

If you wish to use conan versions of some of our dependences, invoke

`conan build conan --build=missing`

before calling the configure script.

To update the lockfile, use

`conan lock create conan`

