# src/macosx/client/ Directory

## Summary
macOS client-specific build configuration headers.

## Details
Contains `config.h` with preprocessor definitions for the macOS client build:
- macOS Application Support data directory path
- SDL, SDL_image, SDL_mixer, and libpng availability flags

## Directory Structure

```
.
└── config.h    # macOS client build configuration
```
